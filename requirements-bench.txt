# Installed on the control plane (your Mac). Pure client + analysis — runs fine on Apple Silicon.
openai>=1.30.0      # used only as an HTTP client for the OpenAI-compatible vLLM API
aiohttp>=3.9.0
pandas>=2.0.0
matplotlib>=3.8.0
numpy>=1.26.0
tabulate>=0.9.0
